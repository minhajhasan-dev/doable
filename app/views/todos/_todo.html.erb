<div id="<%= dom_id todo %>" class="flex items-start space-x-4">
  <!-- Status Icon -->
  <div class="flex-shrink-0 mt-0.5">
    <% if todo.completed? %>
      <span class="w-8 h-8 flex items-center justify-center bg-emerald-100 text-emerald-600 rounded-full text-sm font-bold">✓</span>
    <% else %>
      <span class="w-8 h-8 flex items-center justify-center bg-gray-100 text-gray-400 rounded-full border-2 border-gray-300"></span>
    <% end %>
  </div>

  <!-- Content -->
  <div class="flex-1 min-w-0">
    <div class="flex items-center gap-3 mb-1">
      <h3 class="text-lg font-semibold text-gray-900 <%= 'line-through text-gray-400' if todo.completed? %>">
        <%= todo.name %>
      </h3>
      <% if todo.priority.present? %>
        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
          <%= case todo.priority
              when 1 then 'bg-red-100 text-red-700'
              when 2 then 'bg-amber-100 text-amber-700'
              when 3 then 'bg-blue-100 text-blue-700'
              else 'bg-gray-100 text-gray-600'
              end %>">
          Priority <%= todo.priority %>
        </span>
      <% end %>
    </div>

    <% if todo.description.present? %>
      <p class="text-sm text-gray-600 mb-2"><%= todo.description %></p>
    <% end %>

    <div class="flex items-center space-x-3 text-xs text-gray-500">
      <span class="inline-flex items-center gap-1">
        <%= heroicon "folder", variant: :solid, options: { class: "w-4 h-4 text-indigo-500" } %>
        <%= link_to todo.project.name, project_path(todo.project),
          class: "text-indigo-600 hover:text-indigo-800 font-medium transition-colors duration-200" %>
      </span>
      <span class="inline-flex items-center gap-1">
        <% if todo.completed? %>
          <span class="text-emerald-600 font-medium">Completed</span>
        <% else %>
          <span class="text-amber-600 font-medium">In Progress</span>
        <% end %>
      </span>
    </div>
  </div>
</div>
